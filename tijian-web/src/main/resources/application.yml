server:
  port: 8282

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://192.168.88.128:3306/tijian?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: 1234

mybatis-plus:
  global-config:
    banner: false                       # 关闭mp的banner打印
  configuration:
    map-underscore-to-camel-case: true  # 开启下滑线转驼峰
  mapper-locations: classpath*:com/tijian/**/mapping/*.xml